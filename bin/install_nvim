#!/usr/bin/env bash
BIN_DIR="$(dirname "${BASH_SOURCE[0]}")"
source "$BIN_DIR"/dev-env/runner && run

# install script does not have execute permissions, so sourcing here instead
source "$BIN_DIR"/../src/install.sh