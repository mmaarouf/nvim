#!/usr/bin/env bash
REPO_DIR="$(dirname "${BASH_SOURCE[0]}")/.."
source "$REPO_DIR"/dev-env/runner && run "$@"

curl https://raw.githubusercontent.com/mmaarouf/nvim/refs/heads/main/install.sh | bash
bash_unit "$REPO_DIR"/test/test_vim_installation.sh