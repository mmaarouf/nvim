#!/usr/bin/env bash
REPO_DIR="$(dirname "${BASH_SOURCE[0]}")/.."
source "$REPO_DIR"/dev-env/runner && run "$@"

mkdir -p "$REPO_DIR"/out/

tar czf "$REPO_DIR"/out/nvim.tar.gz -C "$REPO_DIR" nvim-config installer/install.sh