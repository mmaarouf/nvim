#!/usr/bin/env bash
REPO_DIR="$(dirname "${BASH_SOURCE[0]}")/.."
source "$REPO_DIR"/dev-env/runner && run "$@"

# install script does not have execute permissions, so sourcing here instead
source "$REPO_DIR"/installer/install.sh